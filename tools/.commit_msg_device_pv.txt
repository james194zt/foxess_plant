Add live per-PV device gauges scaled to configured max (v0.9.17).

Device tab shows PV1/PV2 Modbus power in one left card (centred or dual layout); arc fill matches live kW vs effective nameplate from PV config. Battery card stays in the right column.
